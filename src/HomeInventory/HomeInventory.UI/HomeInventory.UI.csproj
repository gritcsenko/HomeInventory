<Project Sdk="Uno.Sdk">
  <PropertyGroup>
    <TargetFrameworks>net8.0-android;net8.0-ios;net8.0-maccatalyst;net8.0-windows10.0.19041;net8.0-browserwasm;net8.0-desktop;net8.0</TargetFrameworks>

    <OutputType>Exe</OutputType>
    <UnoSingleProject>true</UnoSingleProject>

    <!-- Display name -->
    <ApplicationTitle>HomeInventory.UI</ApplicationTitle>
    <!-- App Identifier -->
    <ApplicationId>HomeInventory</ApplicationId>
    <!-- Versions -->
    <ApplicationDisplayVersion>1.0</ApplicationDisplayVersion>
    <ApplicationVersion>1</ApplicationVersion>
    <!-- Package Publisher -->
    <ApplicationPublisher>Hrytsenko</ApplicationPublisher>
    <!-- Package Description -->
    <Description>HomeInventory.UI powered by Uno Platform.</Description>
    <!--
      If you encounter this error message:

        error NETSDK1148: A referenced assembly was compiled using a newer version of Microsoft.Windows.SDK.NET.dll.
        Please update to a newer .NET SDK in order to reference this assembly.

      This means that the two packages below must be aligned with the "build" version number of
      the "Microsoft.Windows.SDK.BuildTools" package above, and the "revision" version number
      must be the highest found in https://www.nuget.org/packages/Microsoft.Windows.SDK.NET.Ref.
    -->
    <!-- <WindowsSdkPackageVersion>10.0.22621.28</WindowsSdkPackageVersion> -->

    <!--
      UnoFeatures let's you quickly add and manage implicit package references based on the features you want to use.
      https://aka.platform.uno/singleproject-features
    -->
    <UnoFeatures>
      Lottie;
      MediaElement;
      Hosting;
      Toolkit;
      Logging;
      LoggingSerilog;
      MauiEmbedding;
      Mvvm;
      Configuration;
      Http;
      Serialization;
      Localization;
      AuthenticationOidc;
      Navigation;
      ThemeService;
    </UnoFeatures>
    <NoWarn>$(NoWarn);CA1010</NoWarn>
    <EnableDefaultPageItems>False</EnableDefaultPageItems>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)'=='Debug' or '$(IsUiAutomationMappingEnabled)'=='True'">
    <IsUiAutomationMappingEnabled>True</IsUiAutomationMappingEnabled>
    <DefineConstants>$(DefineConstants);USE_UITESTS</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\HomeInventory.Core\HomeInventory.Core.csproj" />
    <ProjectReference Include="..\HomeInventory.UI.DataContracts\HomeInventory.UI.DataContracts.csproj" />
  </ItemGroup>

  <ItemGroup>
    <UpToDateCheckInput Remove="Presentation\UIShell.xaml" />
  </ItemGroup>

  <ItemGroup>
    <Page Include="Presentation\UIShell.xaml" />
  </ItemGroup>

  <ItemGroup>
    <Compile Update="Presentation\UIShell.xaml.cs">
      <DependentUpon>UIShell.xaml</DependentUpon>
    </Compile>
  </ItemGroup>

</Project>
